{
  "name": "media-core",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-agnostic Pexels media SDK. Zero UI, zero React.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
